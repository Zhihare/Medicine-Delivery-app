const { Pharmacy } = require('./pharmacyModel');
const { Order } = require('./orderModel');
const {Preparations} = require('./preparationsModel');

module.exports = { Pharmacy, Order, Preparations };
